export const appName = 'PaperBell 文档';

/** 与 next.config.mjs 的 basePath 保持一致。 */
export const basePath = '/docs';

/** 站点部署在 basePath `/docs` 下，所以文档树本身挂在应用根路由。 */
export const docsRoute = '/';

/** 主站地址，导航栏用来跳回去。 */
export const siteUrl = 'https://paperbell.cn';

/** 静态搜索索引导出的位置（含 basePath）。 */
export const searchIndexUrl = '/docs/search-index.json';

export const repoUrl = 'https://github.com/PaperBell-Org/Obsidian-PaperBell';
